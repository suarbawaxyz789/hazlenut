pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        maven { url 'https://jitpack.io' }
        maven {
            url 'https://nexus.ninjavan.co/repository/maven-snapshots/'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                username = "ninjavan"

                password = "Eiquoo3Vsheda6SheiQu5Wio"
            }
        }
    }
}
rootProject.name = "Hazelnut"
include ':app'
